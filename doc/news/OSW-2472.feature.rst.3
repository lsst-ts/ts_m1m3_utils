Modify ``ThermocoupleAnalysis`` to use the plane-fit code from the ``ThermalGradients`` class. That fixes the Cartesian gradient error estimate (it was computed from the y coordinates instead of the temperature residuals).
