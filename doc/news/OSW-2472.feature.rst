Add ``ThermocoupleCache`` and ``fit_thermal_gradients`` to cache live M1M3 thermal scanner temperatures and fit thermal gradients from a completed valid set, plus ``M1M3ThermalGradientsDataClient`` (``lsst.ts.m1m3.utils.ess``) - an ESS data client publishing them as ESS.m1m3ThermalGradients telemetry. ``ThermocoupleAnalysis`` now shares the same plane-fit code, which also fixes the Cartesian gradient error estimate (it was computed from the y coordinates instead of the temperature residuals).
